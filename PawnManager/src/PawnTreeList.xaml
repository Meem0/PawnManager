<UserControl x:Class="PawnManager.PawnTreeList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PawnManager"
             xmlns:tree="clr-namespace:PawnManager.TreeList"
             mc:Ignorable="d">
    <tree:TreeList x:Name="TreeList">
        <tree:TreeList.Resources>
            <local:IntToDoubleConverter x:Key="IntToDoubleConverter" />
        </tree:TreeList.Resources>
        <tree:TreeList.View>
            <GridView>
                <GridView.Columns>
                    <GridViewColumn Header="Name" Width="150">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <tree:RowExpander />
                                    <TextBlock Text="{Binding Label}" />
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Value" Width="200">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate DataType="PawnParameter">
                                <ContentControl Content="{Binding}">
                                    <ContentControl.Resources>
                                        <DataTemplate DataType="{x:Type local:PawnCategory}">
                                        </DataTemplate>
                                        <DataTemplate DataType="{x:Type local:PawnParameterName}">
                                            <TextBox Text="{Binding Path=Value}"
                                                     MaxLength="{Binding Path=MaxNameLength}" />
                                        </DataTemplate>
                                        <DataTemplate DataType="{x:Type local:PawnParameterSlider}">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBox Text="{Binding Value}" Width="30" Margin="0,0,5,0" />
                                                <Slider Minimum="{Binding Path=Minimum}"
                                                        Maximum="{Binding Path=Maximum}"
                                                        IsSnapToTickEnabled="True" TickFrequency="1" Width="150"
                                                        Value="{Binding Path=Value, Converter={StaticResource IntToDoubleConverter}}" />
                                            </StackPanel>
                                        </DataTemplate>
                                        <DataTemplate DataType="{x:Type local:PawnParameterDropDown}">
                                            <ComboBox ItemsSource="{Binding Path=Options}" SelectedIndex="{Binding Path=Value}"
                                                      DisplayMemberPath="Label" Width="180" />
                                        </DataTemplate>
                                    </ContentControl.Resources>
                                </ContentControl>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView.Columns>
            </GridView>
        </tree:TreeList.View>
    </tree:TreeList>
</UserControl>
